NAME="pyperfstore"

LOG="$LOG_PATH/$NAME.log"
rm -f $LOG &> /dev/null

function build(){
	true
}

function install(){
	rm -R $PREFIX/lib/python2.7/site-packages/pyperfstore* 1>> $LOG 2>> $LOG

	mkdir -p $PREFIX/tmp/

	cd $SRC_PATH
	tar cfz $PREFIX/tmp/$NAME.tgz $NAME 1>> $LOG 2>> $LOG
}
