NAME="celery"

LOG="$LOG_PATH/$NAME.log"
rm -f $LOG &> /dev/null

function build(){
	true
}

function install(){
	cd $PREFIX
	easy_install_pylib "celery"
	install_python_daemon "$PREFIX/bin/celeryd"
}
